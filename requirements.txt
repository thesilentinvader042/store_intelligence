fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1
sqlalchemy>=2.0.30
alembic>=1.13.1
redis>=5.0.4
httpx>=0.27.0
structlog>=24.1.0
python-dotenv>=1.0.1

# PostgreSQL driver — required when DATABASE_URL is postgresql://
# psycopg2-binary bundles libpq so no system postgres install is needed
psycopg2-binary>=2.9.9

# Detection pipeline
ultralytics>=8.2.18
lap>=0.5.12
opencv-python-headless>=4.9.0.80

# Tests
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.6

# Optional: torchreid for full Re-ID (comment out if not needed for initial run)
# torch>=2.3.0
# torchreid @ git+https://github.com/KaiyangZhou/deep-person-reid.git
